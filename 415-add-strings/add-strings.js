/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var addStrings = function(num1, num2) {
    // let n1= Math.max(num1,num2), n2= Math.min(num1,num2)

    // for(let i)

    return BigInt(num1)+BigInt(num2)+''
    
};